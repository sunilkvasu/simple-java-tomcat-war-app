stage('build') {
    echo "Build stage"
    sleep 2
}
stage('test') {
    echo "Test stage"
    sleep 2
}
stage('quality') {
    echo "Deploy stage"
    sleep 2
}
stage('deploy') {
    echo "Deploy stage"
    sleep 2
}
